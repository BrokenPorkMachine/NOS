# NOS
NitrOS is a small educational microkernel built around the Mach design. The project now
includes a secure boot process that verifies the kernel's SHA-256 hash before
execution. Build scripts also enable stack protection and other hardened compile
flags for both the bootloader and kernel.
